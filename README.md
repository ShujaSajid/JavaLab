# JavaLab
 JAVA Assignments for JMI MCA (SEM 4)
 BY SHUJA SAJID


## Assignment 1 for JAVA CLASS

#### Note From The Instructor
--------------------------------------------------------------------------------------------------
1. Write your programming following OOP paradigms. 
2. Strictly follow the naming convention (Camel case) used in Java.
3. Along with the source code, also submit the test-cases on which your program is tested.
4. Submission can be done either softcopy of source-code or scanned copy of hand-written programs.
5. Strictly follow the submission deadline in order to avoid loss of IA marks.
--------------------------------------------------------------------------------------------------
#### Question
Create a Rectangle class which stores only the Cartesian coordinates of the four corners of the rectangle. The constructor calls a set function that accepts four sets of coordinates and verified each of these is in the first quadrant with no single x or y coordinate larger than 50.0. The set function also verified that the supplied coordinate do, in fact, specify a rectangle. Provide member functions that calculate the length, width, perimeters and area. The length is the larger of the two dimensions. Include a predicate function square that determines whether the rectangle is a square.
Write a tester program to test the working of the Rectangle class.


## Assignment 2 for JAVA CLASS (1 Mar)

Define a class Graph that should model a Graph data structure and store its adjacency matrix, and have default (adjacency matrix should be a null matrix) as well as parameterized constructors. Graph class also has two method display() that displays the adjacency matrix, and isConnected() as predicate method that checks whether given graph is connected or not. Create two another classes namely DirectedGraph and WeightedGraph, both inherits Graph. DirectedGraph has two methods indegree() and outdegree() that calculates and indegree and outdegree of each node in the DirectedGraph. Similarly, WeightedGraph has a method shortestPath() that finds the shortest path between two given nodes using any of the algorithm known to you.
Write a Test program to demonstrate the working of all the three classes in your main program.

Due : 7 March.